<!-- Main component for a primary marketing message or call to action -->


<div class="jumbotron">
  <h2>What's your story this day?</h2>
  <% if current_user %>
  <p>Welcome <%= current_user.first_name %>, glad to see you again.</p>

  <a class="btn btn-lg btn-primary" href="/messages/new" role="button">Write Post</a>


  <% else %>

  <p>For the poet, the lover, and the fighter that lives inside us all, because sometimes they just need to be heard.</p>
  <p>
    <a class="btn btn-lg btn-primary" href="/sessions/new" role="button">Login</a>
    <a class="btn btn-lg btn-primary" href="/users/new" role="button">Sign Up &raquo;</a>
  </p>
  <% end %>

</div>

<% if current_user %>

<div class="row marketing">
  <div class="col-lg-8 col-lg-offset-2">
    <table class="table">
      <tr> hello </tr>
      <% Message.all.each do |message| %>
        <%if message.parent_id.nil? %>
          <tr class="index-row">
          <h4><a href="/messages/<%= message.id %>"><%= message.message %></a></h4>
          </tr>
        <% end %>
      <% end %>
    </table>
  </div>
</div>

<% end %>